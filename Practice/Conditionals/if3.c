#include <stdio.h>

int main(){
    int myAge = 26;
    int VoteAge = 18;

    if(myAge >= VoteAge){
        printf("You are allowed to vote");
    } else {
        printf("Not old enough to vote");
    }
}