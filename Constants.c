/*Constants are variables that when declared they cannot be changed*/

#include <stdio.h>

int main(){

    const int WorkHours = 24;

    const  float pi = 3.14;

    printf("A day has %d\n",WorkHours);

    printf("The value for pi is %.2f",pi);

    return 0;
}