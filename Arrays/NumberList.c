// arrays - used to store multiple values in a single variable.
//Like any number variable, we should specify the data type.

#include <stdio.h>

int main(){
    int numbers[] = {25,50, 75, 100};
    
    return 0;
}